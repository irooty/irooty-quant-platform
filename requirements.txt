# 推荐或要求 Python 3.11 环境运行本项目
# 建议使用 pyenv、Anaconda、Miniconda 或官方安装包管理你的 Python 版本

# 核心科学计算库 - 确保与 NumPy 2.x 兼容
numpy>=2.0.0
pandas>=2.0.0
scipy>=1.11.0

# 机器学习相关 - 确保与 NumPy 2.x 兼容
scikit-learn>=1.5.0
hmmlearn>=0.3.0
joblib>=1.3.0

# Qlib相关依赖
pyqlib[all]
jupyter

# 数据处理 - 确保与 NumPy 2.x 兼容
pyarrow>=12.0.0
modin>=0.20.0
dask>=2023.0.0
polars>=0.20.0

# 日志和工具
loguru>=0.7.0
pyyaml>=6.0

# 金融数据接口
baostock>=0.8.8
tushare>=1.2.89

# 可视化相关 - 确保与 NumPy 2.x 兼容
matplotlib>=3.7.0
seaborn>=0.12.0

# 开发工具
pytest>=7.0.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.0.0

# 其他工具
tqdm>=4.65.0
python-dotenv>=1.0.0

# 异步和并发相关
aiohttp>=3.8.0
asyncio>=3.4.3

# 重试机制
backoff>=2.2.0

# 类型提示支持
typing-extensions>=4.5.0

# 新增 rich 依赖
rich>=13.0.0

# 定时任务调度
schedule>=1.2.0